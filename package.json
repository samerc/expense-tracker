{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Full-featured expense tracking application with YNAB-style envelope budgeting",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix web-admin\"",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web-admin",
    "install:all": "npm install && npm install --prefix server && npm install --prefix web-admin",
    "build": "npm run build --prefix web-admin",
    "start": "npm start --prefix server",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
